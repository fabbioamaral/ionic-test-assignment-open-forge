import { ViewUserDirective } from './view-user.directive';

describe('ViewUserDirective', () => {
  it('should create an instance', () => {
    const directive = new ViewUserDirective();
    expect(directive).toBeTruthy();
  });
});
